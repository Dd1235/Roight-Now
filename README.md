# Roight Now

Should I do it roight now?

## Run Locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The Vite base is set to `./`, so the generated `dist/` assets can be hosted from GitHub Pages project paths.
